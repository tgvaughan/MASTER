<!-- Simulate an inheritance tree from the SIR epidemic model,
     demonstrating the LineageSampler post-processor. -->

<beast version='2.0' namespace='master.beast:beast.core.parameter'>
  <run spec='InheritanceTrajectory'
       samplePopulationSizes="true"
       verbosity="1">

    <model spec='InheritanceModel' id='model'>
      <population spec='Population' id='S' populationName='S'/>
      <population spec='Population' id='I' populationName='I'/>
      <population spec='Population' id='R' populationName='R'/>
      
      <reaction spec='InheritanceReaction' reactionName="Infection" rate="0.001">
	S + I -> 2I
      </reaction>
      <reaction spec='InheritanceReaction' reactionName="Recovery" rate="0.2">
	I -> R
      </reaction>
    </model>
    
    <initialState spec='InitState'>
      <populationSize spec='PopulationSize' population='@S' size='999'/>
      <lineageSeed spec='Individual' population='@I'/>
    </initialState>

    <!-- Simulation will terminate when no lineages remain -->
    <lineageEndCondition spec='LineageEndCondition' nLineages="0"/>
    
    <!-- Randomly sample a sub-set of the lineages extant at time 10 -->
    <inheritancePostProcessor spec='LineageSampler' nSamples="10" samplingTime="10" markAnnotation="sampled"/>
    
    <output spec='NexusOutput' fileName='SIRTreeSample_output.nexus'/>
    <output spec='JsonOutput' fileName='SIRTreeSample_output.json'/>
  </run>
</beast>
