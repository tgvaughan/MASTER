<!-- Use Stochastic Logistic model as the basis of a computational complexity test. -->

<beast version='2.0' namespace='master.beast:beast.core.parameter'>

  <run spec='InheritanceTrajectory'
       simulationTime='20'
       samplePopulationSizes="true"
       nSamples="1000"
       verbosity='1'>
        
    <model spec='InheritanceModel' id='model'>
      <population spec='Population' populationName="X" id='X'/>
      <reaction spec='InheritanceReaction' reactionName="Birth" rate="1.0">
	X -> 2X
      </reaction>
      <reaction spec='InheritanceReaction' reactionName="Death" rate="0.00003">
	2X -> X
      </reaction>
    </model>

    <initialState spec='InitState'>
      <!--<populationSize spec='PopulationSize' population="@X" size='1'/>-->
      <lineageSeed spec='Individual' population="@X"/>
    </initialState>
        
    <output spec='JsonOutput' fileName='complexityIG_out.json'/>
    <output spec='NexusOutput' fileName='complexityIG_out.nexus'/>
  </run>
</beast>
